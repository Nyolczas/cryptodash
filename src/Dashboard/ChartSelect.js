import styled from 'styled-components';
import { backgroundColor2, color } from '../Shared/Styles';

export default styled.select`
  ${backgroundColor2}
  color: white;
  float: right;
  border: none !important;
`;
